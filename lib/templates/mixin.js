define(function (require) {
  'use strict';

  return with<%= _.classify(moduleName) %>;

  function with<%= _.classify(moduleName) %>() {
    this.defaultAttrs({

    });

    this.after('initialize', function () {

    });
  }

});
